pipeline {
    agent any
    stages {
        stage("ckeckout") {
            sh "echo checkout done"
        }
        stage("build") {
            sh "echo build is done"
        }
        stage("deploy") {
            sh "echo deploy is done"
        }
    }
}
